# Use shared storage for multi-node stability.
data_root_dir: /work2/hg76zate-BA/HCNOSPClF_combined_datasets_filtered0.001
run_dir: /work2/hg76zate-BA/fairchem_runs
#run_dir: ${oc.env:PWD}/runs
account: null
qos: null
mode: SLURM
device: CUDA
ranks_per_node: 8 #8 ursprünglich
dataloader_workers: 0
debug: true
mem_gb: "0"
cpus_per_task: 4 #4 ursprünglich
timeout_hr: "1" #48 ursprünglich
partition: clara
max_atoms: 360
max_neighbors: 30    # K4L2 is small enough to handle full neighbor count
num_moe_experts: 32
cutoff_radius: 7.0   # Å; matched to K10L4 backbone default
setup_commands: []
